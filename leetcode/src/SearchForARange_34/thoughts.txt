就是"搜索已排序有重复数组中某元素最大及最小所在位置"
我和optimal思路一致,搜索最小时,hi下推, 搜索最大时lo上推. 
但在搜索最大位置时,为了避开lo=mid造成的死循环问题,我使用了break,而optimal
在循环中将mid 变成 (lo+hi)/2+1. 这样lo = mid就不会死循环.
同时也为二分搜索提供思路, 不必死板遵循mid = (lo+hi)>>1的中点选择,
二分搜索本质是对数级地排除搜索段,mid =  (lo+hi)/2+1同样能够做到这一点.