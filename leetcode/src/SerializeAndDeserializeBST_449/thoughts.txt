思路:
先序遍历建字符串,但不为空节点插入字符,因为可以根据父节点判断节点大小是否合理.

建二叉树时同样先序,通过父节点大小维护一个大小关系,不符就说明当前为空.
算法优化角度: 
两个重要特点都与Java语言特性有关:
1. int 应该转变成4个字节的内容存储到字符串中,这样更紧凑,且能形成"天然"的分隔符.然而这样的
操作在Java中不常见....
2. 如果可以传递引用,就可以不用将字符串分解成队列,而是在函数间很方便的传递当前游标值.
强行传递引用也是可以的, int[].